import React from "react";
import { View, Text } from "react-native";

const CreatePost = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: 'white' }}>
            <Text>creating dream</Text>
        </View>
    );
}

export default CreatePost;
